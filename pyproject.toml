[project]
name = "mutation-microscope"
version = "1.0.0"
description = "Interactive Multi-Scale Genomic Observatory for AlphaGenome Variant Effect Predictions"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "alphagenome>=0.9.0",
    "pandas>=2.0.0",
]
